{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "toast",
  "description": "Capture HUD — it only renders what Rust hands it",
  "windows": ["toast"],
  "permissions": ["core:event:default"]
}
